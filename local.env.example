# Copy to local.env (Chandra reads it via pydantic-settings) or export in shell.

# Max generated tokens per page
MAX_OUTPUT_TOKENS=12384

# vLLM client
VLLM_API_KEY=EMPTY
VLLM_API_BASE=http://127.0.0.1:8000/v1
VLLM_MODEL_NAME=chandra
